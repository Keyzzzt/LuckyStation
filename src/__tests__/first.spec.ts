// import {getProducts getProductById, createReview} from '@src/Controllers/productController'

// it('should pass', () => {
//   const result = getProducts(req, res, next)
//   expect(result).toBe('24')
// })
